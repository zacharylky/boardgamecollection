<!DOCTYPE html>
<html>
    <head>
        <% include ./partial/head.ejs %>
    </head>
    <body>
        <h1>Board Game page</h1>
        <div class="container">
            <h2><%=game.name; %></h2>
                <div class="smallContainer">

                    <!-- Image, Description, Price -->
                    <img class="show-bg" src="<%=game.img%>"></br>
                    <%=game.description; %> </br>
                    <b>Rating: </b><%=game.rating; %> </br>
                    <b>No. of Players: </b><%=game.playersMin; %> to <%=game.playersMax; %></br>
                    <b>Weight/Complexity: </b><%=game.weight; %></br>
                    <b>Rating: </b><%=game.rating; %></br>
                    <b>Tags: </b><%=tag.name %></br>

                    <!-- Edit Button -->
                    <form action="/games/<%=game.id; %>/edit">
                        <input type="submit" value="Edit"/>
                    </form>

                    <!-- Delete button -->
                    <form action="/games/<%=game.id; %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                </div>
        </div>
        <nav>
            <a href="/games">Back to Collection</a>
        </nav>
    </body>
    <footer>
    </footer>
</html>